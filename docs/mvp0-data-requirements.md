# MVP-0 — Exigences données

## Par famille pilote

Articles, attributs, familles, nomenclatures + rebuts, gammes + temps cycle/setup, centres/postes, calendriers + exceptions, TRS, liaisons BOM↔opération, historique OF (prévu/réel qty/durée/dates/rebut).

## Provenance

Chaque lot : `REAL` | `SIMULATED` | `TO_CONFIRM`.

Les données `SIMULATED` interdisent un GO final (`INSUFFICIENT_DATA`).
